<section class="hero py-16">
  <div class="container mx-auto flex items-center justify-between">
    <div class="w-1/2">
      <h6 class="text-lg"><em>Are you hungry?</em></h6>
      <h1 class="text-6xl font-bold">Don't Wait !</h1>
      <button
        class="py-2 px-6 rounded-full text-white font-bold mt-4 btn-primary"
      >
        Order Now
      </button>
    </div>
    <div class="w-1/2">
      <img src="/img/hero-pizza.png" />
    </div>
  </div>
</section>
<section class="menu container mx-auto py-8" id="main">
  <h1 class="text-xl font-bold mb-8">All Pizzas</h1>
  <div class="grid grid-cols-4 col-gap-12 row-gap-16">
    <% pizzas.forEach((dish)=>{ %>
    <div class="w-64 py-4">
      <img class="h-40 mb-4 mx-auto" src="/img/<%= dish.image %>" alt="" />
      <div class="text-center">
        <h2 class="mb-4 text-lg"><%= dish.name %></h2>
        <span class="size py-1 px-4 rounded-full uppercase text-xs"
          ><%= dish.size %></span
        >
        <div class="flex items-center justify-around mt-6">
          <span class="font-bold text-lg">₹<%= dish.price %></span>
          <button
            data-pizza="<%=JSON.stringify(dish)%>"
            class="add-to-cart py-1 px-6 rounded-full flex items-center font-bold"
          >
            <span>+</span>
            <span class="ml-4 mr-2">Add</span>
          </button>
        </div>
      </div>
    </div>
    <%})%>
  </div>
</section>
